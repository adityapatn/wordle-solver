#The purpose of this program is to build a general-purpose word solver that takes a partial input and returns a list of the words that it could be.

partial = input("Enter partial word using * as a wildcard: ")
word_length = len(partial)